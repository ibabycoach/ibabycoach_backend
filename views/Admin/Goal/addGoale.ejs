
<%- include('../common/header') %>
<%- include('../common/sidebar') %>
  

<style>
#day-error-placeholder {
  min-height: 20px; /* Reserve space for error message */
}

.jqval-error {
  color: red;
  font-size: 0.875rem;
  margin-top: 5px;
  position: relative;
}


</style>
<!-- BEGIN: Content-->
<div class="app-content content ">
  <div class="content-wrapper container-xxl p-0">
    <div class="content-body">
      <section id="multiple-column-form">
        <div class="row justify-content-center">
          <div class="col-md-10 col-12 ">
            <div class="card">
              <div class="card-header">
                <h4 class="card-title"> Add Goal</h4>
              </div>
           
              <div class="card-body" style="margin-top: 20px" >
                <form class="form form-vertical" id="add_validator" action="/saveGoal" method="POST" enctype="multipart/form-data">
                    <div class="mb-1 row">
                      <div class="col-sm-2">
                        <label class="col-form-label new_lable" for="name">Goal Name</label>
                      </div>
                      <div class="col-sm-10">
                        <input type="text" id="name" class="form-control" name="name" value="" required>
                      </div>
                    </div>
                    <div class="mb-1 row">
                      <div class="col-sm-2">
                        <label class="col-form-label new_lable" for="name">Title</label>
                      </div>
                      <div class="col-sm-10">
                        <input type="text" id="name" class="form-control" name="title" value="" required>
                      </div>
                    </div>
                     
                    <div class="mb-1 row">
                      <div class="col-sm-2">
                        <label for="range-1a">Min Age:</label>
                      </div>
                      <div class="col-sm-10">
                        <input type="range" name="min_age" id="range-1a" min="0" max="100" value="0">
                        <span id="min_age_value"></span>
                        <small id="min_age_error" class="text-danger" style="display: none;">Minimum age cannot be 0</small>
                      </div>
                    
                      <div class="col-sm-2">
                        <label for="range-1b">Max Age:</label>
                      </div>
                      <div class="col-sm-10">
                        <input type="range" name="max_age" id="range-1b" min="0" max="100" value="0">
                        <span id="max_age_value"></span>
                      </div>
                    </div>
                    
           <div class="mb-1 row">
  <div class="col-sm-2">
    <label id="dayLabel" class="col-form-label new_label">Day</label>
  </div>
  <div class="col-sm-10">
    <div id="day-checkbox-group" class="position-relative">
      <input class="form-check-input day-checkbox" type="checkbox" id="mon" name="day" value="Mon">
      <label class="form-check-label me-2" for="mon">Mon</label>

      <input class="form-check-input day-checkbox" type="checkbox" id="tue" name="day" value="Tue">
      <label class="form-check-label me-2" for="tue">Tue</label>

      <input class="form-check-input day-checkbox" type="checkbox" id="wed" name="day" value="Wed">
      <label class="form-check-label me-2" for="wed">Wed</label>

      <input class="form-check-input day-checkbox" type="checkbox" id="thu" name="day" value="Thu">
      <label class="form-check-label me-2" for="thu">Thu</label>

      <input class="form-check-input day-checkbox" type="checkbox" id="fri" name="day" value="Fri">
      <label class="form-check-label me-2" for="fri">Fri</label>

      <input class="form-check-input day-checkbox" type="checkbox" id="sat" name="day" value="Sat">
      <label class="form-check-label me-2" for="sat">Sat</label>

      <input class="form-check-input day-checkbox" type="checkbox" id="sun" name="day" value="Sun">
      <label class="form-check-label me-2" for="sun">Sun</label>
    </div>
    <!-- Placeholder for error message to avoid shifting -->
    <div id="day-error-placeholder"></div>
  </div>
</div>

                    <div class="mb-1 row">
                      <div class="col-sm-2">
                        <label class="col-form-label new_lable" for="name">Description</label>
                      </div>
                      <div class="col-sm-10">
                        <input type="text" id="name" class="form-control" name="description" value="" required>
                      </div>
                    </div>
                     
                    <div class="mb-1 row">
                      <div class="col-sm-2">
                        <label class="col-form-label new_lable" for="price">Image</label>
                      </div>
                      <div class="col-sm-10">
                        <input type="file" id="image" name="image" accept="image/*" class="form-control" name="image" value="" required>
                      </div>
                    </div>
                    <div class="col-12">
                    <div class="col-sm-9 offset-sm-5">
                      <button type="submit" class="btn btn-primary waves-effect waves-float waves-light">Add</button>
                    </div>
                  </div>
                </form>
              </div>
            </div>
          </div>
        </div>
      </section>
    </div>
  </div>
</div>
<div class="sidenav-overlay"></div>
<div class="drag-target"></div>
<%- include('../common/footer') %>

 <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.3/jquery.validate.min.js" 
integrity="sha512-37T7leoNS06R80c8Ulq7cdCDU5MNQBwlYoy1TX/WUsLFC2eYNqtKlV0QjH7r8JpG/S0GUMZwebnVFLPd6SU5yg==" 
crossorigin="anonymous" referrerpolicy="no-referrer">
</script>

<script>
  // JavaScript code to dynamically update age values

  // Function to update max age value display and ensure it's not less than min age
function updateMaxAge() {
    var minAgeInput = document.getElementById("range-1a");
    var maxAgeInput = document.getElementById("range-1b");
    var maxAgeValueSpan = document.getElementById("max_age_value");
    var minAge = parseInt(minAgeInput.value);
    var maxAge = parseInt(maxAgeInput.value);

    // Ensure max age is not less than min age
    if (maxAge < minAge) {
        maxAge = minAge;
        maxAgeInput.value = maxAge;
    }

    if (maxAge < 12) {
        maxAgeValueSpan.textContent = maxAge + " weeks";
    } else if (maxAge >= 12 && maxAge < 52) {
        maxAgeValueSpan.textContent = Math.floor(maxAge / 4) + " months";
    } else {
        maxAgeValueSpan.textContent = Math.floor(maxAge / 52) + " year";
    }
}

// Function to update min age value display and synchronize max age input
function updateMinAge() {
    var minAgeInput = document.getElementById("range-1a");
    var maxAgeInput = document.getElementById("range-1b");
    var minAgeValueSpan = document.getElementById("min_age_value");
    var minAge = parseInt(minAgeInput.value);
    var maxAge = parseInt(maxAgeInput.value);

    // Synchronize max age input with min age input
    if (maxAge < minAge) {
        maxAgeInput.value = minAge;
        updateMaxAge();
    }

    if (minAge < 12) {
        minAgeValueSpan.textContent = minAge + " weeks";
    } else if (minAge >= 12 && minAge < 52) {
        minAgeValueSpan.textContent = Math.floor(minAge / 4) + " months";
    } else {
        minAgeValueSpan.textContent = Math.floor(minAge / 52) + " year";
    }
}

// Add event listeners to range inputs
document.getElementById("range-1a").addEventListener("input", updateMinAge);
document.getElementById("range-1b").addEventListener("input", updateMaxAge);

// Initial update of age values
updateMinAge();
updateMaxAge();

</script>

<script>
  const checkboxes = document.querySelectorAll('.day-checkbox');
  const label = document.getElementById('dayLabel');

  function updateLabel() {
    const selected = Array.from(checkboxes).filter(cb => cb.checked).length;
    label.textContent = selected > 1 ? 'Days' : 'Day';
  }

  checkboxes.forEach(cb => cb.addEventListener('change', updateLabel));
</script>

<script>
  const form = document.querySelector('form');
  const minAgeInput = document.getElementById('range-1a');
  const minAgeError = document.getElementById('min_age_error');
  const minAgeValue = document.getElementById('min_age_value');

  const maxAgeInput = document.getElementById('range-1b');
  const maxAgeValue = document.getElementById('max_age_value');

  // Update live display
  minAgeInput.addEventListener('input', () => {
    minAgeValue.textContent = minAgeInput.value;
    if (parseInt(minAgeInput.value) > 0) {
      minAgeError.style.display = 'none';
    }
  });

  maxAgeInput.addEventListener('input', () => {
    maxAgeValue.textContent = maxAgeInput.value;
  });

  form.addEventListener('submit', function (e) {
    if (parseInt(minAgeInput.value) === 0) {
      e.preventDefault();
      minAgeError.style.display = 'block';
    } else {
      minAgeError.style.display = 'none';
    }
  });
</script>


<script>
 $(document).ready(function() {
  // Add a custom validation method to allow alphabetic characters, spaces, and commas
  jQuery.validator.addMethod("alphabetWithCommas", function(value, element) {
    return this.optional(element) || /^[a-zA-Z\s,]*$/.test(value);  // Allow letters, spaces, and commas
  }, "Please enter alphabets, commas, and spaces only (no numbers or other special characters)");

  jQuery.validator.addMethod("noSpace", function(value, element) {
    return value == '' || value.trim().length != 0;
  }, "No space allowed at beginning or end");

  jQuery.validator.addMethod("validImage", function(value, element, param) {
    return this.optional(element) || /\.(jpe?g|gif|png)$/i.test(value);
  }, 'Only valid image files (JPEG, GIF, PNG) are allowed.');

  $('#add_validator').validate({
    rules: {
      name: {
        required: true,
        noSpace: true,
        maxlength: 20
      },
      title:{
        required: true,
        noSpace: true,
        maxlength: 20
      },
      min_age: {
        required: true
      },
      max_age: {
        required: true
      },
      description: {
        required: true,
        noSpace: true,
        maxlength: 300
      },
      day: {
        required: true,
        noSpace: true,
        alphabetWithCommas: true,  // Updated to new validation rule that accepts commas
        maxlength: 60
      },
      image: {
        // required: true,
        validImage: true
      }
    },
    messages: {
      name: {
        required: "This field is required"
      },
      title: {
        required: "This field is required"
      },
      day: {
        required: "This field is required",
        alphabetWithCommas: "Day should contain only alphabets, commas, and spaces"
      },
      description: {
        required: "This field is required"
      }
    },
    errorElement: "div",
    errorClass: "jqval-error",
    errorPlacement: function(error, element) {
      error.insertAfter(element);
      error.slideDown(400);
      error.animate({
        opacity: 1
      }, {
        queue: false,
        duration: 700
      });
    },
    submitHandler: function(form) {
      var error = $(".assign_error_div").text();
      if (!error) {
        form.submit();
      }
    }
  });
  
  // Fix the alphabetOnly validation in the second script
  // Add custom validation for the 'Day' field in profile form (if it exists)
  if ($('#profile').length > 0) {
    $('#profile').validate({
      rules: {
        day: {
          required: true,
          noSpace: true,
          alphabetWithCommas: true, // Updated to allow commas
          maxlength: 60
        },
        phone: {
          required: true,
          noSpace: true,
          maxlength: 12
        }
      },
      messages: {
        day: {
          required: "This field is required",
          alphabetWithCommas: "Day should contain only alphabets, commas, and spaces"
        },
        phone: {
          required: "This field is required"
        }
      },
      errorElement: "div",
      errorClass: "jqval-error",
      errorPlacement: function(error, element) {
        error.insertAfter(element);
        error.slideDown(400);
        error.animate({
          opacity: 1
        }, {
          queue: false,
          duration: 700
        });
      },
      submitHandler: function(form) {
        var error = $(".assign_error_div").text();
        if (!error) {
          form.submit();
        }
      }
    });
  }
});
</script>