<%- include('../common/header') %>
  <%- include('../common/sidebar') %>

    <!-- BEGIN: Content-->
    <div class="app-content content ">
      <div class="content-wrapper container-xxl p-0">
        <div class="content-body">
          <section id="multiple-column-form">
            <div class="row justify-content-center">
              <div class="col-md-10 col-12 ">
                <div class="card">
                  <div class="card-header">
                    <h4 class="card-title">Update Goal</h4>
                  </div>
                  <br>
                  <div class="card-body">
                    <form class="form form-vertical" id="add_validator" action="/updateGole" method="POST"
                      enctype="multipart/form-data">
                      <div class="row">
                        <div class="col-md-4">
                          <div class="profile-widget-header text-center">
                            <img src="<%=Goaldetail.image%>" id="output2" alt=" "
                              class="ms-md-0 mx-auto img-thumbnail user_bigimg">
                            <input onchange="loadFile(event,'output2')" type="file" id="imgInp" name="image" />
                          </div>
                        </div>
                        <div class="col-md-8">
                          <div class="mb-1 row">
                            <div class="col-sm-2">
                              <label class="col-form-label new_lable" for="name">Name</label>
                            </div>
                            <div class="col-sm-10">
                              <input type="text" id="name" class="form-control" name="name" value="<%=Goaldetail.name%> ">
                              <input type="hidden" name="id" value="<%=Goaldetail.id%>">
                            </div>
                          </div>
                          <div class="mb-1 row">
                            <div class="col-sm-2">
                              <label class="col-form-label new_lable" for="price">Title</label>
                            </div>
                            <div class="col-sm-10">
                              <input type="text" id="price" class="form-control" name="title"
                                value="<%=Goaldetail.title%> ">
                            </div>
                          </div>
                          <div class="mb-1 row">
                            <div class="col-sm-2">
                                <label for="range-1a">Min Age:</label>
                            </div>
                            <div class="col-sm-10">
                                <input type="range" name="min_age" id="range-1a" min="0" max="100" value="<%=Goaldetail.min_age%>" data-popup-enabled="true" data-show-value="true">
                                <span id="min_age_value"></span> <!-- Display min age value here -->
                            </div>
                            <div class="col-sm-2">
                                <label for="range-1b">Max Age:</label>
                            </div>
                            <div class="col-sm-10">
                                <input type="range" name="max_age" id="range-1b" min="0" max="100" value="<%=Goaldetail.max_age%>" data-popup-enabled="true" data-show-value="true">
                                <span id="max_age_value"></span> <!-- Display max age value here -->
                            </div>
                        </div>
                        <div class="mb-1 row">
                          <div class="col-sm-2">
                            <label class="col-form-label new_lable" for="name">Day</label>
                          </div>
                          <div class="col-sm-10">
                            <input type="text" id="name" class="form-control" name="day" value="<%=Goaldetail.day%> ">
                          </div>
                        </div>
                        <!-- <div class="mb-1 row">
                          <div class="col-sm-2">
                            <label class="col-form-label new_lable" for="tenure">Date</label>
                          </div>
                          <div class="col-sm-10">
                            <input type="date" id="tenure" class="form-control" name="date" value="<%= new Date(Goaldetail.date).toLocaleDateString('en-US', { day: '2-digit', month: 'long', year: 'numeric' }) %>">
                          </div>
                        </div> -->
                        <div class="mb-1 row">
                          <div class="col-sm-2">
                            <label class="col-form-label new_lable" for="tenure">Description</label>
                          </div>
                          <div class="col-sm-10">
                            <textarea class="form-control" id="content" rows="5" name="description" required> <%=Goaldetail.description%>  </textarea>
                          </div>
                        </div>
                        </div>
                      </div>
                      <div class="col-12">
                        <div class="col-sm-9 offset-sm-5">
                          <button type="submit"
                            class="btn btn-primary waves-effect waves-float waves-light">Update</button>
                          <!-- <a href="/subscriptionList" class="btn btn-primary float-right" title="Edit Profile"> Back </a>   -->
                        </div>
                      </div>
                    </form>
                  </div>
                </div>
              </div>
            </div>
          </section>
        </div>
      </div>
    </div>
    <div class="sidenav-overlay"></div>
    <div class="drag-target"></div>
    <%- include('../common/footer') %>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.3/jquery.validate.min.js" 
integrity="sha512-37T7leoNS06R80c8Ulq7cdCDU5MNQBwlYoy1TX/WUsLFC2eYNqtKlV0QjH7r8JpG/S0GUMZwebnVFLPd6SU5yg==" 
crossorigin="anonymous" referrerpolicy="no-referrer">
</script>

<script>
  // JavaScript code to dynamically update age values

  // Function to update max age value display and ensure it's not less than min age
function updateMaxAge() {
    var minAgeInput = document.getElementById("range-1a");
    var maxAgeInput = document.getElementById("range-1b");
    var maxAgeValueSpan = document.getElementById("max_age_value");
    var minAge = parseInt(minAgeInput.value);
    var maxAge = parseInt(maxAgeInput.value);

    // Ensure max age is not less than min age
    if (maxAge < minAge) {
        maxAge = minAge;
        maxAgeInput.value = maxAge;
    }

    if (maxAge < 12) {
        maxAgeValueSpan.textContent = maxAge + " weeks";
    } else if (maxAge >= 12 && maxAge < 52) {
        maxAgeValueSpan.textContent = Math.floor(maxAge / 4) + " months";
    } else {
        maxAgeValueSpan.textContent = Math.floor(maxAge / 52) + " year";
    }
}

// Function to update min age value display and synchronize max age input
function updateMinAge() {
    var minAgeInput = document.getElementById("range-1a");
    var maxAgeInput = document.getElementById("range-1b");
    var minAgeValueSpan = document.getElementById("min_age_value");
    var minAge = parseInt(minAgeInput.value);
    var maxAge = parseInt(maxAgeInput.value);

    // Synchronize max age input with min age input
    if (maxAge < minAge) {
        maxAgeInput.value = minAge;
        updateMaxAge();
    }

    if (minAge < 12) {
        minAgeValueSpan.textContent = minAge + " weeks";
    } else if (minAge >= 12 && minAge < 52) {
        minAgeValueSpan.textContent = Math.floor(minAge / 4) + " months";
    } else {
        minAgeValueSpan.textContent = Math.floor(minAge / 52) + " year";
    }
}

// Add event listeners to range inputs
document.getElementById("range-1a").addEventListener("input", updateMinAge);
document.getElementById("range-1b").addEventListener("input", updateMaxAge);

// Initial update of age values
updateMinAge();
updateMaxAge();

</script>

<script>
  $(document).ready(function() {
    jQuery.validator.addMethod("noSpace", function(value, element) {
      return value == '' || value.trim().length != 0;
    }, "No space allowed");

    jQuery.validator.addMethod("validImage", function(value, element, param) {
      return this.optional(element) || /\.(jpe?g|gif|png)$/i.test(value);
    }, 'Only valid image files (JPEG, GIF, PNG) are allowed.');
  

    $('#add_validator').validate({
      rules: {
        name: {
          required: true,
          noSpace: true,
          maxlength: 20
        },
        title:{
          required: true,
          noSpace: true,
          maxlength: 20
        },
        min_age: {
          required: true
        },
        max_age: {
          required: true
        },
        description: {
          required: true,
          noSpace: true,
          maxlength: 60
        },
        day:{
          required: true,
          noSpace: true,
        },
        image: {
          required: true,
          validImage: true
        }
      },
      errorElement: "div",
      errorClass: "jqval-error",
      errorPlacement: function(error, element) {
        error.insertAfter(element);
        error.slideDown(400);
        error.animate({
          opacity: 1
        }, {
          queue: false,
          duration: 700
        });
      },
      submitHandler: function(form) {
        var error = $(".assign_error_div").text();
        if (!error) {
          form.submit();
        }
      }
    });
  });
</script>